package com.hzkc.parent.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.hzkc.parent.greendao.entity.AppDataBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "APP_DATA_BEAN".
*/
public class AppDataBeanDao extends AbstractDao<AppDataBean, Long> {

    public static final String TABLENAME = "APP_DATA_BEAN";

    /**
     * Properties of entity AppDataBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property Childuuid = new Property(1, String.class, "childuuid", false, "CHILDUUID");
        public final static Property Apppackgename = new Property(2, String.class, "apppackgename", false, "APPPACKGENAME");
        public final static Property Appname = new Property(3, String.class, "appname", false, "APPNAME");
        public final static Property Appwhitelist = new Property(4, String.class, "appwhitelist", false, "APPWHITELIST");
        public final static Property Issystem = new Property(5, String.class, "issystem", false, "ISSYSTEM");
    };


    public AppDataBeanDao(DaoConfig config) {
        super(config);
    }
    
    public AppDataBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"APP_DATA_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: _id
                "\"CHILDUUID\" TEXT," + // 1: childuuid
                "\"APPPACKGENAME\" TEXT," + // 2: apppackgename
                "\"APPNAME\" TEXT," + // 3: appname
                "\"APPWHITELIST\" TEXT," + // 4: appwhitelist
                "\"ISSYSTEM\" TEXT);"); // 5: issystem
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"APP_DATA_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AppDataBean entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String childuuid = entity.getChilduuid();
        if (childuuid != null) {
            stmt.bindString(2, childuuid);
        }
 
        String apppackgename = entity.getApppackgename();
        if (apppackgename != null) {
            stmt.bindString(3, apppackgename);
        }
 
        String appname = entity.getAppname();
        if (appname != null) {
            stmt.bindString(4, appname);
        }
 
        String appwhitelist = entity.getAppwhitelist();
        if (appwhitelist != null) {
            stmt.bindString(5, appwhitelist);
        }
 
        String issystem = entity.getIssystem();
        if (issystem != null) {
            stmt.bindString(6, issystem);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AppDataBean entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String childuuid = entity.getChilduuid();
        if (childuuid != null) {
            stmt.bindString(2, childuuid);
        }
 
        String apppackgename = entity.getApppackgename();
        if (apppackgename != null) {
            stmt.bindString(3, apppackgename);
        }
 
        String appname = entity.getAppname();
        if (appname != null) {
            stmt.bindString(4, appname);
        }
 
        String appwhitelist = entity.getAppwhitelist();
        if (appwhitelist != null) {
            stmt.bindString(5, appwhitelist);
        }
 
        String issystem = entity.getIssystem();
        if (issystem != null) {
            stmt.bindString(6, issystem);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AppDataBean readEntity(Cursor cursor, int offset) {
        AppDataBean entity = new AppDataBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // childuuid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // apppackgename
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // appname
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // appwhitelist
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // issystem
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AppDataBean entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setChilduuid(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setApppackgename(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setAppname(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setAppwhitelist(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setIssystem(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AppDataBean entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AppDataBean entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
